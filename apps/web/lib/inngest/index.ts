/**
 * Inngest functions and client — discovery trigger, scheduled prioritization.
 * @see apps/web/app/api/inngest/route.ts — serve endpoint
 */
export { inngest } from "./client";
export { functions } from "./functions";
